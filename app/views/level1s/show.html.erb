<div class="span7">
	<%= render 'shared/draw1' %>
</div>

<div class="well well-large span4">
	<%= render 'shared/item_show' %>

	<% if logged_in? %>
	<%= link_to 'edit', edit_first_second_path(params[:first_id], @items.first.id), class: 'btn btn-primary' %>
	<%= link_to 'delete', first_second_path(params[:first_id], params[:id]), :confirm => 'Are you sure?', :method => :delete, class: 'btn btn-primary ' %>
	<% end %>
</div>