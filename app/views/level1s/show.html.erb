<div class="span8">
	<%= render 'shared/draw1' %>
</div>

<div class="well well-large span4">

	<h2><%= @items.first.title %></h2>

	<p class="lead"><%= @items.first.overview %></p>

	<p><%= @items.first.description %></p>
	<%= link_to 'go back', ia_path, class: 'btn btn-primary' %>
	<%= link_to 'edit', edit_first_second_path(params[:first_id], @items.first.id), class: 'btn btn-primary' %>
	<%= link_to 'delete', first_path(@item), :confirm => 'Are you sure?', :method => :delete, class: 'btn btn-primary ' %>
</div>